const {resolve} = require, _local = id => resolve (id, { paths: [ process.cwd(), __dirname ] })
const require = id => module.require (_local(id)) // overwrite require so that navigation in VS Code works below

// eslint-disable-next-line cds-dk/use-api-require, cds-dk/use-relative-require
module.exports = require('@sap/cds/bin/test')
